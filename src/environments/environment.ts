// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

// Import the functions you need from the SDKs you need
//import { initializeApp } from "firebase/app";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
export const environment = {
  production: false,
  apiKey: "AIzaSyCZK4kGd7j2iBnKjdq-SN7eZKAo3uy1dd8",
  authDomain: "loginbase-a6a8b.firebaseapp.com",
  databaseURL: "https://loginbase-a6a8b-default-rtdb.firebaseio.com",
  projectId: "loginbase-a6a8b",
  storageBucket: "loginbase-a6a8b.appspot.com",
  messagingSenderId: "530640486925",
  appId: "1:530640486925:web:2fc34ddd55e81da61dcb44"
};

// Initialize Firebase
//const app = initializeApp(environment);
/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
